version: 0.0
os: linux
files:
  - source: ./
    destination: /tmp/release/
hooks:
  BeforeInstall:
    - Location: scripts/beforeInstall.sh
      runas: root
  Install:
    - Location: scripts/copy.sh
      runas: root
